CREATE OR REPLACE VIEW AI_FEATURE_HUB.V_ACCOUNT_BALANCE AS SELECT ACCOUNT_ID,SUM(IFF(EVENT_TYPE='payment',-AMOUNT,AMOUNT)) AS BALANCE,CURRENCY FROM AI_FEATURE_HUB.BILLING_EVENTS GROUP BY ACCOUNT_ID,CURRENCY;
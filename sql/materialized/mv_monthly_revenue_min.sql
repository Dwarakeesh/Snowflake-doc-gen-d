CREATE OR REPLACE MATERIALIZED VIEW AI_FEATURE_HUB.MV_MONTHLY_REVENUE AS SELECT DATE_TRUNC('MONTH',CREATED_AT) AS INVOICE_MONTH,SUM(TOTAL) AS MONTHLY_REVENUE FROM AI_FEATURE_HUB.SUBSCRIPTION_INVOICES GROUP BY 1;
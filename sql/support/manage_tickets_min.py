from snowflake.snowpark import Session;import uuid;def create_ticket(session:Session,account_id:str,subject:str,description:str):ticket_id=str(uuid.uuid4());session.sql("INSERT INTO AI_FEATURE_HUB.SUPPORT_TICKETS(TICKET_ID,ACCOUNT_ID,SUBJECT,DESCRIPTION,STATUS) VALUES (%s,%s,%s,%s,'OPEN')",(ticket_id,account_id,subject,description)).collect();return {"ticket_id":ticket_id};def add_comment(session:Session,ticket_id:str,user_id:str,comment:str):comment_id=str(uuid.uuid4());session.sql("INSERT INTO AI_FEATURE_HUB.TICKET_COMMENTS(COMMENT_ID,TICKET_ID,USER_ID,COMMENT) VALUES (%s,%s,%s,%s)",(comment_id,ticket_id,user_id,comment)).collect();return {"comment_id":comment_id}
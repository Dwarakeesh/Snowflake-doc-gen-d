CREATE OR REPLACE TABLE AI_FEATURE_HUB.ALERT_RULES (RULE_ID STRING PRIMARY KEY, ALERT_KEY STRING, THRESHOLD NUMBER, TIME_WINDOW_MINUTES NUMBER, COOLDOWN_MINUTES NUMBER, IS_ENABLED BOOLEAN, NOTIFICATION_TARGET VARIANT, META VARIANT);